## Confetti-burst request — S_Effects teleports the shared one-shot
## GPUParticles2D to `position` and restarts it, then removes the component.
## Stamped on round wins (at your deck), successful slaps, and the K.O.
class_name C_Burst
extends Component

@export var position: Vector2 = Vector2.ZERO
